# Timezone
GENERIC_TIMEZONE=Europe/Rome

# PostgreSQL Database
POSTGRES_USER=n8n
POSTGRES_PASSWORD=ChangeThisStrongPassword123!
POSTGRES_DB=n8n

# n8n Security Keys (Generate new ones for production!)
# Generate with: openssl rand -hex 32
N8N_ENCRYPTION_KEY=your-encryption-key-here-change-this
N8N_USER_MANAGEMENT_JWT_SECRET=your-jwt-secret-here-change-this

# n8n Basic Auth (Optional - remove if using built-in user management)
N8N_BASIC_AUTH_ACTIVE=false
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=ChangeThisPassword123!

# Traefik Dashboard Basic Auth
# Generate with: echo $(htpasswd -nB admin) | sed -e s/\\$/\\$\\$/g
# Default is admin:admin (CHANGE THIS!)
TRAEFIK_USER_PASS=admin:$$2y$$10$$hHsBeqgxLD6F9/FLJqr0C.DKf8CNlLIX5KYDV5kfvslexs6QVvieO

# Cloudflare API for Let's Encrypt DNS Challenge
CF_API_EMAIL=your-cloudflare-email@example.com
CF_API_KEY=your-cloudflare-global-api-key